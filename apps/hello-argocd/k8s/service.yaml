apiVersion: v1
kind: Service
metadata:
  name: hello-argocd
  namespace: default
  labels:
    app: hello-argocd
spec:
  selector:
    app: hello-argocd
  ports:
    - port: 8080
      targetPort: 8080